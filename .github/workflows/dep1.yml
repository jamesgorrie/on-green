name: dep1
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        with:
          script: |
            const sleep = new Promise((reject, resolve) => {
              setTimeout(() => resolve(), 3000);
            });
            const t = await sleep;
            console.log(context)